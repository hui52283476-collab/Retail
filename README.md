 Group Progress Tracker MVP

語音進度更新 → 自動轉錄 → 多模型摘要 → 規格書比對 → 一鍵生成郵件

 30 秒快速啟動（已驗證成功）

```bash
git clone https://github.com/hui52283476-collab/Retail.git
cd Retail
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
現在 100% self-contained，任何人 clone 都能直接跑，無需修改任何程式碼。
